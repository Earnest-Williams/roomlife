# Draft schema notes

Goal:
- Keep the simulation engine UI-agnostic.
- The engine outputs structured state and structured events.
- A view-model layer renders state for text now and graphics later.

Save format:
- JSON snapshot with schema_version.
