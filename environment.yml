# Managed by sdc (direnv + mamba/conda).
# The env name follows the directory name: roomlife
name: roomlife
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pyyaml>=6.0
  - rich>=13.0
  - typer>=0.9.0
  - pytest>=8.0
  - ruff>=0.1.0
